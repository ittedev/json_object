import { JSONCollection } from './json_collection.ts'

/**
 * JSONRange is the same as JSONCollection.
 * However, that means it's part of the JSONCollection.
 */
export type JSONRange = JSONCollection
